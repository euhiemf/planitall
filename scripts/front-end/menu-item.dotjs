{{ var link = it.stripLink(it.link); }}

<li id="{{= it.id }}" data-selectable-id='{{= it.link }}' data-index='{{= it.index }}' class='selectable{{? it.haveSubMenu }} submenual{{??}} nosubmenu{{?}}'><a href='#{{= it.link }}'>{{= it.title }}</a>{{? it.haveSubMenu }}

	<ul class='sub-menu'>
		{{ var sublink; }}
		{{~it.submenus :value}}
			{{ sublink = link + '/' + it.stripLink(value.link); }}
			<li data-selectable-id='{{= it.sublink }}' class='selectable'><a href='#{{= it.sublink }}'>{{= value.title }}</a></li>
		{{~}}
	</ul>

{{?}}</li>